apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devboard-pvc
  namespace: devboard
spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi