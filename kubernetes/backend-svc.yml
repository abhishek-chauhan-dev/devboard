apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend
  name: backend
  namespace: devboard
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: backend
status:
  loadBalancer: {}
